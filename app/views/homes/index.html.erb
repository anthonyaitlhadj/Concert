<div class="container">
  <div class="col-md-12">
    <%#	 @concerts.each do |concert| %>
      <div class="col-md-12">
        <div class="panel panel-default">
          <div class="panel-heading"><h2>Derniers concerts</h2></div>
          <div class="panel-body">
            <div class="row">
              <% @shows.each do |show| %>
                <div class="col-md-6">
                  <h3><%= show.name %></h3>
                  <p>Places dispo: <%= show.place %></</p>
                  <p>Prix: <%= show.price %></p><br>
                  <%= link_to "voir", show_path(show.id)%>
                </div>
                <div class="col-md-6">Image</div>
              <%end%>
            </div>
          </div>
        </div>
      </div>
  </div>
</div>