<%if logged_in?%>
	Logged in as: <b><%= current_user.pseudo %></b> with an ID of <b><%= current_user.id %></b><br>
<%end%>
<%= form_for @comment, url: show_comments_url(@show, @comment) do |comment| %>
    User: <%= comment.label current_user.pseudo %><br>
    <%=	 comment.text_area :contenu %><br>
    <%= comment.submit "Envoyer" %>
<%	end %>